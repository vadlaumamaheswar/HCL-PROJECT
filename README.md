# Code Review Analytics (Java + MongoDB)

## 📘 Project Overview
**Code Review Analytics** is a Java-based console application built using **Maven** and integrated with **MongoDB**.  
The project analyzes and manages code review data, demonstrating clean project structuring, database connectivity, and execution using Maven.

This repository follows the standard **Maven directory structure**.

---

## 📂 Project Structure

```
CODE_REVIEW_ANALYTICS/
│
├── .dist/
│
├── src/
│   └── main/
│       └── java/
│           └── com/
│               ├── CodeReview.java
│               ├── CodeReviewAnalyticsApp.java
│               └── MongoDBUtil.java
│
├── target/
│   ├── classes/
│   │   └── com/
│   │       ├── CodeReview.class
│   │       ├── CodeReviewAnalyticsApp.class
│   │       └── MongoDBUtil.class
│   │
│   ├── generated-sources/
│   ├── maven-status/
│   │   └── maven-compiler-plugin/
│   │       ├── createdFiles.lst
│   │       └── inputFiles.lst
│   │
│   └── test-classes/
│
├── pom.xml
└── README.md
```

---

## ⚙️ Technologies Used
- **Java 21**
- **Maven**
- **MongoDB**
- **MongoDB Java Driver (Sync)**
- **VS Code / IntelliJ IDEA**

---

## 📦 Maven Configuration
The project uses Maven for dependency management and execution.

**Key details from `pom.xml`:**
- Group ID: `com.codereview`
- Artifact ID: `code-review-analytics`
- Java Version: `21`
- Main Class: `com.codereview.CodeReviewAnalyticsApp`

---

## ▶️ How to Run the Project

### 1️⃣ Prerequisites
- Java JDK 21 installed
- Maven installed
- MongoDB running locally (default port: `27017`)

### 2️⃣ Clone the Repository
```bash
git clone <your-repository-url>
cd code-review-analytics
```

### 3️⃣ Build the Project
```bash
mvn clean compile
```

### 4️⃣ Run the Application
```bash
mvn exec:java
```

---

## 🧩 Class Description

### `CodeReview.java`
- Model class representing a code review entity
- Holds review-related data

### `MongoDBUtil.java`
- Handles MongoDB connection setup
- Provides database utility methods

### `CodeReviewAnalyticsApp.java`
- Main entry point of the application
- Controls program flow and analytics logic

---

## ✅ Features
- Clean Maven project structure
- MongoDB database integration
- Console-based execution
- Modular Java classes
- Easy build and run using Maven

---

## 📌 Notes
- The `target/` directory is auto-generated by Maven.
- Do not manually edit `.class` files.
- Update MongoDB connection settings if required.

---

## 👨‍💻 Author
   **v umamaheswar**

---

## 📄 License
This project is for **educational and academic purposes**.
